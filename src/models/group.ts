import { Image } from './image';

export interface Group {
    id: string;
    name: string;
    desc?: string;
    _id: string;
    // members: string[];
    // images: Image[];
    // admin: string;
}