export interface Image {
    id: string;
    name: string;
    extension: string;
    size: number;
    thumbnailUrl: string;
    publicId: string;
    owner: string;
    providers: string[];
    _id: string;
}